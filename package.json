{
  "name": "csdn-meeting-dashboards",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:meeting": "npm --prefix meeting-dashboard run dev",
    "dev:ops": "npm --prefix ops-dashboard run dev",
    "build": "npm run build:meeting && npm run build:ops",
    "build:meeting": "npm --prefix meeting-dashboard run build",
    "build:ops": "npm --prefix ops-dashboard run build"
  }
}
